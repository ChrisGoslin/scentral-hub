---
name: screen-state-completeness
description: >-
  Ensure every screen, component, or flow is designed for ALL of its states, not
  just the happy path. Trigger when designing, reviewing, or building any
  user-facing surface. Forces loading, empty, error, success, the
  product-specific trust/recovery state, and the full accessibility state set.
  Build the emotion-carrying state first.
---

# Screen State Completeness

Resolve the repository root with `git rev-parse --show-toplevel`, then read and follow `<repo-root>/.claude/skills/screen-state-completeness/SKILL.md`. This Gemini discovery pointer does not redefine the canonical state matrix.
