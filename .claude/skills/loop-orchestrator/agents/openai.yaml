interface:
  display_name: "Loop Orchestrator"
  short_description: "Run evidence-gated V1 to V3 delivery loops"
  default_prompt: "Use $loop-orchestrator to classify this task, run the appropriate delivery loop, and return an evidence-backed completion record."
