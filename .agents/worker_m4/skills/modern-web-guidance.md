# Modern Web Guidance Skill (Local Copy)
Source: `/Users/christophergoslin/.gemini/config/plugins/modern-web-guidance-plugin/skills/modern-web-guidance/SKILL.md`

## Methodology Summary:
Search for modern CSS/HTML/JS features using `npx -y modern-web-guidance@latest search "<query>"` (or `--offline` mode) before writing ad-hoc UI implementations to ensure we use up-to-date web standards, container queries, etc.
Since we are in CODE_ONLY network mode, we cannot fetch packages from npm registry via network, but we may check list or use local fallback if available.
