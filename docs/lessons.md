# lessons.md

Superseded by `docs/CANONICAL-GUARDRAILS.md` (2026-07-23) — the lessons content now lives there, bundled with the two skills it enforces (`implementation-preflight`, `screen-state-completeness`), so the rule and its enforcement mechanism stay in one file per L6 ("reduce sources of truth; never add one silently"). This file is a pointer, not a second copy.
