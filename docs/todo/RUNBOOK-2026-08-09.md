# Runbook — 2026-08-09

> **Archived on 2026-08-10 — do not run top to bottom.** This runbook mixed local nota.
> work with pushes, destructive recovery, shared agent configuration, and other product
> repositories. It grants no future authority. Reuse only a separately re-verified,
> explicitly approved step whose targets stay within the current task scope.

The sections below preserve the old plan for provenance. They are not an executable
queue and their historical "done" conditions are not current evidence.

Source docs (don't go hunting; they're linked from each step):
`docs/HANDOVER-2026-08-02-memory-durability.md` ·
`docs/todo/claude-code-prompts-2026-08-02.md` ·
`docs/todo/alignment-sweep-addenda-2026-08-02.md` ·
`docs/todo/spike-buzz-and-alignment-sweep-2026-08-09.md`

---

## Step 0 — Inspect the historical blocker · **Terminal**

Inspect `.git/index.lock` and active Git processes first. Do not delete a lock based on
this document; a live process may own it. Confirm staleness and obtain explicit approval
before removal.

---

## Step 1 — Preferences · **Claude Settings → Personal preferences** · 2 minutes

Replace **only** the first line of your existing preferences — the one beginning
*"Before every task, read CLAUDE.md and PROJECTS.md from ~/.claude/"*. Everything from
*"Work with me as a non-technical engineering manager"* onward stays untouched.

```
At the start of every session, before anything else: call
request_cowork_directory for ~/Projects, then run the
cowork-session-preflight skill. Do this even if I invoked a slash-command
or named a skill — that does not exempt the session from the gate.

Canon lives in ~/Projects/claude-global/ — CLAUDE.md, PROJECTS.md,
LESSONS.md, profile.md. NOT ~/.claude/ and NOT ~/ai-studio; neither holds
canon and neither is reachable from Cowork. Always check PROJECTS.md
before making any plan.

Never ask me where my files are or to run find/ls in Terminal to locate a
path. Mount and look.

Cowork authors; Claude Code verifies. Cowork cannot delete files, so it
cannot commit and cannot clean up after git. From Cowork the ONLY permitted
git commands are non-index reads: log, show, rev-list, ls-files, cat-file,
branch --list. NEVER git status, git diff against the worktree, add, commit,
stash or rebase — they take .git/index.lock and strand it, blocking every
tool in the repo for everyone.

Session handovers are repo artifacts: <repo>/docs/HANDOVER-<date>-<topic>.md,
committed. Never /tmp or /home/claude — both are wiped. If it isn't
committed, it didn't happen.
```

**Done when:** saved. Test at step 8.

---

## Step 2 — Verification · **Claude Code** · gates everything below

> You are verifying another agent's work, not extending it. Be adversarial. The session
> that produced this ran in Cowork, which cannot delete files, cannot run git, and cannot
> run `npm run build` — so **nothing below was executed against a build**. Treat every
> claim as unverified until you prove it.
>
> Read `docs/HANDOVER-2026-08-02-memory-durability.md`, then verify each claim and report
> **Verified / Unverified / False** per line. Do not fix anything yet.
>
> 1. `docs/index.md` "External Context" cites only reachable `~/Projects/claude-global/`
>    paths, and no `~/.claude/*.md`.
> 2. `claude-global/LESSONS.md` contains GL-7 through GL-10; `docs/lessons.md` contains
>    L45, L46, L47, L58, L59. Each has all four fields (What happened · Rule · Remedy
>    built in · Enforced by).
> 3. **Every `Enforced by:` in those lessons names a mechanism that actually exists and
>    runs where claimed.** Highest-risk item — the authoring session wrote four false
>    `Enforced by` claims and caught them only on self-review. Open each named file.
> 4. `alignment-sweep` contains both an "Integrity assertions — added 2026-08-02" block
>    and a "Scope correction — added 2026-08-02" block in **all three** trees
>    (`.claude`, `.agents`, `.gemini`), byte-identical, 251 lines each.
> 5. `repo-tidy` contains step 7 (lesson-ID integrity) in all three trees.
> 6. `branch-hygiene` has Step 0 (capability probe) and a diverged-main section, and the
>    Step 0 probe does **not** create a file it cannot remove.
> 7. Run every bash snippet added to those skills verbatim. Report actual output.
>    Expected: lesson IDs clean (59/59); tree counts 29/11/10.
> 8. `bash -n docs/todo/commit-2026-08-02.sh` passes and every path in its `PATHS` array
>    exists.
> 9. **Run `npx tsc --noEmit` and `npm run build`.** Cowork could not.
>
> Then one overall verdict: SAFE TO COMMIT / DO NOT COMMIT — with reasons.

**Done when:** verdict reads SAFE TO COMMIT. If not, stop and report.

---

## Step 3 — Commit · **Claude Code** · local only, reversible

> Prompt 0 returned SAFE TO COMMIT. This step does **not** rebase, push, or delete.
>
> 1. Recovery ref first — makes everything after this a one-command undo:
>    ```bash
>    git branch backup/pre-durability-2026-08-02 main
>    git rev-parse --short backup/pre-durability-2026-08-02
>    ```
> 2. `bash docs/todo/commit-2026-08-02.sh`
> 3. Also add the two files written after that script:
>    ```bash
>    git add -- docs/todo/spike-buzz-and-alignment-sweep-2026-08-09.md \
>                docs/todo/RUNBOOK-2026-08-09.md
>    git commit -m "docs(todo): add buzz spike and 2026-08-09 runbook" -- \
>                docs/todo/spike-buzz-and-alignment-sweep-2026-08-09.md \
>                docs/todo/RUNBOOK-2026-08-09.md
>    ```
> 4. `git show --stat HEAD` — confirm only intended paths landed.
> 5. Confirm `.loop_probe` is gone.
> 6. **Stop. Report both SHAs. Do not push.**

---

## Step 4 — Sync the unattended sweep · **ARCHIVED AI Ops proposal**

> Do not execute from this repo. Account-level and scheduled skill changes are shared
> AI Ops work and require fresh explicit approval.

> `alignment-sweep` exists in **five** places. Three repo trees were updated 2026-08-02.
> Two were not, because they live outside `~/Projects`:
>
> - `~/Claude/Scheduled/monthly-alignment-sweep/SKILL.md` — **the only copy that runs
>   unattended.** It ran 2026-08-01 and caught none of that week's findings.
> - The Cowork account-level skill.
>
> Both have a shorter, divergent base (127 lines vs 251). **Append, never overwrite.**
> Exact blocks and command: `docs/todo/alignment-sweep-addenda-2026-08-02.md`.
>
> Then add a **self-check pass** so the sweep audits its own enforcement monthly: verify
> all five copies carry the integrity block; confirm `.husky/pre-push` exists and contains
> the canon checks; assert `docs/lessons.md` has no duplicate IDs; confirm no canon doc
> cites `~/.claude/` or `~/ai-studio` outside a guardrail warning. Report any failure as
> HIGH. **If the sweep reports zero findings, state explicitly which checks ran** — a
> silent pass is indistinguishable from a job that isn't running.
>
> Finally trigger one manual run and confirm it reports real findings.

**Done when:** all five copies carry the block, and a manual run produces named output.

---

## Step 5 — Reconcile main · **ARCHIVED; fresh explicit approval required**

> Prerequisite: step 3 complete, `backup/pre-durability-2026-08-02` exists.
>
> The `[ahead 12, behind 4]` figure came from a sandbox that could not fetch. **Unverified.**
> Establish real state first:
> ```bash
> git fetch --all
> git rev-list --left-right --count origin/main...main
> git log --oneline main..origin/main
> git diff --name-only main...origin/main | sort > /tmp/remote.txt
> git diff --name-only origin/main...main | sort > /tmp/local.txt
> comm -12 /tmp/remote.txt /tmp/local.txt
> ```
> Decide from that output:
> - **behind 0** → fast-forward push. Preferred.
> - **behind N, empty overlap** → rebase is low-risk. Proceed.
> - **behind N, non-empty overlap** → **stop, report the overlapping files.**
>
> If proceeding: `git pull --rebase origin main`, then **`npm run build` (mandatory — a
> rebase can produce a broken tree neither side had)**, then `git push origin main`.
> Never `--force`. On conflict: `git rebase --abort` and report.
> On conflict, use `git rebase --abort`. Do not use `git reset --hard` in a shared
> worktree; it can erase unrelated uncommitted work.
>
> Then the stale branch (verified merged, so `-d` is safe — never `-D`):
> `git branch -d integration/homepage-brand-and-fixes`

---

## Step 6 — Skill tiering · **ARCHIVED cross-silo proposal**

> Do not execute from this repo. This step targets other product repositories and shared
> agent configuration; each target needs separate explicit scope.

Copy **Prompt 2** from `docs/todo/claude-code-prompts-2026-08-02.md` — it's ~50 lines and
too long to duplicate here. It classifies all 29 skills into account / baseline /
repo-specific tiers, reconciles the seven already duplicated across two tiers, and syncs a
5–8 skill baseline to every repo. **This also closes the Abundance question.**

**Done when:** baseline count matches across all nine trees.

---

## Step 7 — Pre-push canon guard · **Claude Code** · the safety net

Copy **Prompt 5** from `docs/todo/claude-code-prompts-2026-08-02.md`. Promotes three canon
checks into `.husky/pre-push` — lesson-ID collisions, dead `~/.claude` pointers, skill-tree
parity.

**Read its carve-out carefully.** Without it the hook blocks every push forever, because
`LESSONS.md` legitimately names the dead path in GL-3/GL-7/GL-8. Prompt 5 requires testing
both directions.

---

## Step 8 — Verify the preferences took · **new Cowork session** · 30 seconds

Open a fresh Cowork session and type exactly:

```
where are we
```

**Expected:** it mounts `~/Projects` unprompted and emits a `── DEFINITION OF READY ──`
block. **If it asks where your files are**, step 1 didn't save — redo it.

This is the only test that tells you whether any of the above changed future sessions.

---

## Step 9 — Buzz disposition · **you** · one decision

`~/Projects/buzz` is `github.com/block/buzz` — Block's OSS project, not your IP, 7.8GB.
Not a candidate for `claude-global/` promotion (hard-siloing rule). Either:

- **Keep** as a reference clone → add a one-line row to `claude-global/PROJECTS.md` under a
  "Reference clones (not mine)" heading so no future session mistakes it for portfolio work.
- **Delete** → no deletion is authorized by this document. Reconfirm the exact clone,
  active processes, recoverability, and explicit user approval in a separately scoped task.

Then optionally run Part 1 step 4 of the spike (which session mis-clone'd it) and add the
guardrail: a pre-push check failing on any untracked directory >100MB at repo root.

---

## Summary

| Step | Where | Blocking? |
|---|---|---|
| 0 Clear lock | Terminal | yes |
| 1 Preferences | Settings | no — but nothing else fixes future sessions |
| 2 Verify | Claude Code | **gates 3–7** |
| 3 Commit | Claude Code | gates 5 |
| 4 Sync sweep | Claude Code | no |
| 5 Reconcile main | Claude Code | no |
| 6 Skill tiering | Claude Code | no |
| 7 Pre-push guard | Claude Code | no |
| 8 Test preferences | new Cowork session | no |
| 9 Buzz decision | you | no |

Steps 0, 1, 8, 9 are yours (~4 minutes total). Everything else is Claude Code.
